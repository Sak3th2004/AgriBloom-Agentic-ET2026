# ==============================================================================
# AgriBloom V2 Backend — CPU deployment requirements (free HF Spaces / Docker)
# Trimmed from requirements.txt: CPU-only torch (no CUDA wheels — free hosts
# have no GPU), and training/dev-only tools dropped (kaggle, openai-whisper —
# faster-whisper is the STT primary). Everything else matches requirements.txt
# so behavior is identical, just lighter and CPU-only.
# ==============================================================================

# --- Core Agent Framework ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# --- Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Deep Learning (CPU — free hosting has no GPU) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# --- Transformers & Models ---
transformers>=4.40.0
huggingface-hub>=0.23.0
accelerate>=0.30.0
safetensors>=0.4.0
efficientnet-pytorch>=0.7.1

# --- GenAI (free-tier providers) ---
google-generativeai>=0.8.0

# --- RAG ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- ONNX Runtime (CPU) — offline inference path ---
onnxruntime>=1.17.0
onnx>=1.16.0

# --- Vision & Image Processing ---
Pillow>=10.3.0
opencv-python-headless>=4.9.0

# --- Vision Ensemble: DINOv2 (via transformers) + FAISS KNN ---
faiss-cpu>=1.8.0

# --- Data Science ---
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
scipy>=1.13.0

# --- Visualization (health-recovery chart) ---
plotly>=5.22.0
matplotlib>=3.8.0
kaleido>=0.2.1

# --- PDF Generation ---
reportlab>=4.2.0

# --- Speech & Voice: faster-whisper (STT) + edge-tts (TTS neural) + gTTS fallback ---
faster-whisper>=1.0.0
edge-tts>=6.1.0
gTTS>=2.5.0
pydub>=0.25.1
soundfile>=0.12.1

# --- Multilingual ---
deep-translator>=1.11.0

# --- HTTP & APIs ---
requests>=2.31.0
aiohttp>=3.9.0

# --- Configuration ---
python-dotenv>=1.0.0

# --- Utilities ---
pyyaml>=6.0.0
python-dateutil>=2.9.0
